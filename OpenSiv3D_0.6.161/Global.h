#pragma once

namespace Global {
	// ======================
   // Enum 定義
   // ======================

	enum class Direction {
		LEFT,
		RIGHT
	};

	// ======================
	// Struct 定義
   // ======================
	struct InputState {
		bool left;
		bool right;
		bool jump;
	};

	// ======================
	// 共有変数宣言
	// ======================

	 // ======================
	// 定数
	// ======================
}
